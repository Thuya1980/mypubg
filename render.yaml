services:
  - type: web
    name: pubg-scraper
    env: python
    buildCommand: |
      python3 -m venv .venv
      source .venv/bin/activate
      pip install -r requirements.txt
    startCommand: bash start.sh
    plan: free
